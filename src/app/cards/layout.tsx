import "./cards.css";

/** Routen-Layout /cards: lädt das Cards-Theme (nur für diese Kampagnenseiten). */
export default function CardsLayout({ children }: { children: React.ReactNode }) {
  return children;
}
