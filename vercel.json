{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "buildCommand": "export MIGRATE_URL=\"${DIRECT_DATABASE_URL:-${POSTGRES_URL_NON_POOLING:-${DATABASE_URL_UNPOOLED:-$DATABASE_URL}}}\" && (DATABASE_URL=\"$MIGRATE_URL\" prisma migrate deploy || (sleep 20 && DATABASE_URL=\"$MIGRATE_URL\" prisma migrate deploy)) && next build",
  "crons": [
    {
      "path": "/api/cron/cleanup",
      "schedule": "30 3 * * *"
    },
    {
      "path": "/api/cron/amazon",
      "schedule": "0 6 * * *"
    }
  ]
}
